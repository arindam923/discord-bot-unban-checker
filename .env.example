DISCORD_TOKEN=your_discord_bot_here

# Optional: channel ID for alerts (rate-limit warnings, aggregate heartbeat).
# Falls back to the first guild's system channel if not set.
# ALERT_CHANNEL_ID=123456789012345678

# The bot uses Instagram's own GraphQL API (i.instagram.com) which is free
# and unauthenticated — no API key needed, no monthly quota.
# If Instagram rate-limits your IP (429 errors), lower CHECK_CONCURRENCY
# in main.py or add a proxy via the PROXY_URLS setting below.

# Optional: comma-separated proxy URLs to spread requests across IPs
# Format: http://user:pass@host:port,http://user:pass@host2:port
# PROXY_URLS=http://user:pass@proxy1.example.com:8080
